runtime: python313

entrypoint: gunicorn -b :$PORT api.wsgi

env_variables:
  # This setting is used in settings.py to configure your ALLOWED_HOSTS
  APPENGINE_URL: dontvibecode.uc.r.appspot.com
  
  DB_NAME: "postgres"
  DB_USER: "postgres"
  DB_PASSWORD: "Dev!132347590"
  DB_PORT: "5432"
  DB_HOST: "/cloudsql/dontvibecode:us-central1:dev-db"
  
  GS_BUCKET_NAME: "dontvibecode_dontvibecode-dev"
  
  GEMINI_API_KEY: "AIzaSyDxuQDREo47v36hfL4jio98ZE8DzgUfHqY"
  DEBUG: True
  SECRET_KEY: django-insecure-x@%87^6nr88^n63#y%+mu41&r$%12d$8!%fi3h)67wjbq(l1c5

handlers:
# This configures Google App Engine to serve the files in the app's static
# directory.
- url: /static
  static_dir: static/

# This handler routes all requests not caught above to your main app. It is
# required when static routes are defined, but can be omitted (along with
# the entire handlers section) when there are no static files defined.
- url: /.*
  script: auto
